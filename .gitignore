# Built binary (matches the module name when built at the repo root).
/declare

# Go build & test artifacts.
*.test
*.out
*.exe
*.exe~
*.dll
*.so
*.dylib
coverage.out

# Editor & OS cruft.
.DS_Store
.idea/
.vscode/
*.swp
*~

# Local-only background material (e.g. exported chat transcripts that
# inform the design but are not part of the project's public artifact).
/docs/origins/
